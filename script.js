function startWavePayment(amount) {
    // This script connects to the Wave Checkout API
    // In production, your backend will generate a unique payment link
    alert("Opening Secure Wave Checkout for " + amount);
    
    // For Gambia, use a Wave payment link or a merchant aggregator like Modem Pay
    // Example: window.location.href = "https://wave.com";
}
